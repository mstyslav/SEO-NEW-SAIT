{
  "name": "space-glass-web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "check:routes": "node scripts/check-routes.mjs",
    "check:links": "node scripts/check-links.mjs",
    "check:imports": "node scripts/check-imports.mjs",
    "check:seo": "node scripts/check-seo-pages.mjs",
    "check:production": "npm run check:imports && npm run check:routes && npm run check:links && npm run check:configurator",
    "check:configurator": "node scripts/check-configurator.mjs"
  },
  "dependencies": {
    "astro": "^7.1.0"
  }
}
