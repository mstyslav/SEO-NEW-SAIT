# Astro.redirect(target, 301) in that file is the fallback for any path this
# list does not cover (e.g. if a new page is added before this file is
# regenerated) — it now also renders an instant (0s) meta-refresh, not the
# previous 2s delay, so the fallback itself is safe even if this file is stale.
